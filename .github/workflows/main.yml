name: My First CI Workflow

on: [push]

jobs:
  build:  
    runs-on: ubuntu-latest  

    steps: 
      - name: Checkout Repository Code
        uses: actions/checkout@v3

      - name: Run Sample Command
        run: echo "Hello Anshuman! GitHub Actions is Working 🎉"





        